// Generated by CoffeeScript 1.10.0
var Categories, Posts, Topics, async, privileges;

Posts = require.main.require('./src/posts');

Topics = require.main.require('./src/topics');

Categories = require.main.require('./src/categories');

privileges = require.main.require('./src/privileges');

async = require('async');

(function(Hooks) {
  Hooks.filter = {};
  Hooks.action = {};
  Hooks.filter.groupCreate = function(obj, callback) {
    var data, group, parentCat;
    group = obj.group, data = obj.data;
    if (!data.isClub) {
      return callback(null, obj);
    }
    parentCat = null;
    return async.waterfall([
      function(next) {
        return Categories.create({
          name: data.name,
          description: data.description
        }, next);
      }, function(category, next) {
        var removePrivileges;
        parentCat = category;
        removePrivileges = ['topics:read', 'topics:create', 'topics:reply', 'posts:edit', 'posts:delete', 'topics:delete', 'upload:post:image'];
        return privileges.categories.rescind(removePrivileges, parentCat.cid, 'registered-users', next);
      }, function(next) {
        var removePrivileges;
        removePrivileges = ['find', 'read', 'topics:read'];
        return privileges.categories.rescind(removePrivileges, parentCat.cid, 'guests', next);
      }, function(next) {
        var defaultPrivileges;
        defaultPrivileges = ['find', 'read', 'topics:read', 'topics:create', 'topics:reply', 'posts:edit', 'posts:delete', 'topics:delete', 'upload:post:image', 'etopic:create'];
        return privileges.categories.give(defaultPrivileges, parentCat.cid, data.name, next);
      }, function(next) {
        return async.waterfall([
          function(next) {
            return Categories.create({
              name: '活动',
              isEvent: true,
              parentCid: parentCat.cid
            }, next);
          }, function(cat, next) {
            return Categories.create({
              name: '投票',
              isVote: true,
              parentCid: parentCat.cid
            }, next);
          }, function(cat, next) {
            return Categories.create({
              name: '话题',
              isDiscuss: true,
              parentCid: parentCat.cid
            }, next);
          }
        ], next);
      }
    ], function(err, ret) {
      group.mainCid = parentCat.cid;
      return callback(err, obj);
    });
  };
  return Hooks.filter.categoryCreate = function(obj, callback) {
    var category, data;
    category = obj.category, data = obj.data;
    category.isEvent = data.isEvent;
    category.isVote = data.isVote;
    category.isDiscuss = data.isDiscuss;
    return callback(null, obj);
  };
})(exports);
